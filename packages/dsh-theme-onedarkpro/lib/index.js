/**
 * dsh-theme-onedarkpro — host half (intentionally minimal).
 *
 * The entire feature lives in the web client: it stacks a One Dark Pro
 * token override over the dark palette of the --dsw-* token sheets.
 * No host services, routes, or tools are required.
 *
 * @module dsh-theme-onedarkpro
 */
export const name = 'dsh-theme-onedarkpro'
export const inject = []
export function apply() {}
