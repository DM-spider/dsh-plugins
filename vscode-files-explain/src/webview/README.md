# src/webview/panel-vendored.js

本文件是 `packages/dsh-files/lib/client.js` 的**整文件拷贝,一行未改**。

同步方式:DSH 侧 client.js 更新后,重新拷贝覆盖即可(见仓库根迁移文档
`docs/vscode-files-explain-migration.md` §5.2)。运行环境兼容由
`entry.js` 的 shim 层提供,不要在本文件里打补丁 —— 补丁只能进 `entry.js`。
